<#
    Secrets personnels de dev (mots de passe en clair) : contrairement a dev.local.ps1, ce fichier
    n'est JAMAIS commit (voir .gitignore). Copie-le en "dev.local.secrets.ps1" puis renseigne la
    chaine de connexion Neon ci-dessous (dashboard Neon > Connection Details).
#>

# Utilisee par start-dev.ps1 quand dev.local.ps1 definit $Database = "Prod".
$ProdConnectionString = "postgresql://user:password@host.neon.tech/cortexia_auth?sslmode=require"
